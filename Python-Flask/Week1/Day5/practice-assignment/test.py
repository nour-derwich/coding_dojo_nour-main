from crypt import methods
from operator import methodcaller


class v :
    count=0;
    def __int__(al,wheels,serial,shap,engine):
     al.wheels=wheels
     al.serial=serial
     al.shap=shap
     al.engine=engine
    car=v(4,532,circle,'diseel')
    print(car.shap)
    #
    @classmethod
    def to(cls,pris):
        ruslt=count*pris
        return ruslt
        v.totale(4)